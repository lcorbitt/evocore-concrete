/**
 * Stacks primary nav and hero so together they fill one dynamic viewport on
 * small screens (`100dvh`). From `sm` up, height follows content + hero min-heights.
 */
export const HOME_VIEWPORT_TOP_CLASS =
  "flex min-h-[100dvh] flex-col sm:min-h-0";
