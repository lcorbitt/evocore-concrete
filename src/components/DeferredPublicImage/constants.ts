/** Start loading slightly before the row enters the viewport (px string for `rootMargin`). */
export const DEFERRED_PUBLIC_IMAGE_ROOT_MARGIN = "200px 0px";

export const DEFERRED_PUBLIC_IMAGE_THRESHOLD = 0.01;
